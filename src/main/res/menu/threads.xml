<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
      xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto"
    >
    <item android:id="@+id/search"
          android:icon="@drawable/ic_search"
          android:title="Search"
          app:showAsAction="always"
          app:actionViewClass="android.support.v7.widget.SearchView"
          tools:ignore="AlwaysShowAction"
        />
    <item android:id="@+id/up"
          android:icon="@drawable/ic_up"
          android:title="@string/go_to_top"
          app:showAsAction="never"
        />
    <item android:id="@+id/favorize"
          android:icon="@drawable/ic_favorite"
          android:title="@string/favorize"
          app:showAsAction="never"
        />
    <item android:id="@+id/refresh"
          android:icon="@drawable/ic_refresh"
          android:title="@string/refresh"
          app:showAsAction="never"
        />
    <item android:id="@+id/clear_watchlist"
        android:title="@string/clear_watchlist"
        app:showAsAction="never"
        android:visible="false"
        />
    <item android:id="@+id/down"
          android:title="@string/go_to_bottom"
          app:showAsAction="never"
        />
</menu>